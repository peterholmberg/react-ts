import React from 'react';
import ReactDOM from 'react-dom';
import { AppContainer } from 'react-hot-loader';
import Graphana from 'components/graphana';

const render = Component => {
  ReactDOM.render(
    <AppContainer
  )
}